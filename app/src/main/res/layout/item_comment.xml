<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  >

  <data>
    <import type="ru.profapp.ranobe.common.Constants.ApplicationConstants"/>
    <variable
      name="comment"
      type="ru.profapp.ranobe.network.dto.rulateDTO.RulateComment"/>
  </data>

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/activity_vertical_margin"
    android:orientation="vertical">

    <TextView
      android:id="@+id/tV_comment"
      drawableSide="@{ApplicationConstants.DRAWABLE_LEFT}"
      drawableUrl="@{comment.avatar}"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:drawablePadding="5dp"
      android:text="@{comment.body}"
      tools:drawableLeft="@tools:sample/avatars"
      tools:text="@tools:sample/lorem"
      />

    <TextView
      android:id="@+id/date"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="@{comment.author}"
      tools:text="@tools:sample/date/mmddyy"
      />

    <View style="@style/divider"/>
  </LinearLayout>
</layout>
